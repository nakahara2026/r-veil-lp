# R VEIL — 「Q&A」ページ 設計書 (qa.md)

> このファイルは**下層ページ「Q&A」（`src/pages/qa.astro`）固有**の構成・実装方針をまとめたもの。全ページ共通のルール（カラー・タイポグラフィ・レイアウトの基本方針・共通コンポーネント仕様等）は[design.md](design.md)を参照。

> **ステータス**：実装済み（`src/pages/qa.astro`）。dev serverで表示・アコーディオン開閉挙動を確認済み。

## 1. ページ概要

| 項目 | 内容 |
|---|---|
| ページ名 | Q&A（よくあるご質問） |
| URL | `/qa/`（独立した別ページ） |
| デザインカンプ | `Q&A.png`（1920×2600、閉じた状態）・`Q&A_アコーディオン開.png`（1920×4500、開いた状態）。プロジェクトルート、`.gitignore`対象に追加要（top.png/benefit.pngと同様の運用） |
| 目的・ゴール | ご注文・製作状況・配送・支払い・返品交換・商品についてのFAQを、2階層アコーディオンで提供する |
| データソース | ユーザーから提供された「FAQアコーディオン実装仕様書」に記載のテキストをそのまま採用（Excelの「FAQ」タブ由来） |

---

## 2. 階層構造・データ

- 親（カテゴリー）6件 × 子（Q）1〜8件、合計28問
- カテゴリー：ご注文について／製作状況の確認について／配送・送料　お届け日数について／支払い方法について／返品・交換・キャンセルについて／商品について
- データは`qa.astro`内の`<script>`に`faqData`というJS配列（`{ category, questions: [{ q, a: [段落1, 段落2, ...] }] }`）として直書き。**HTMLは手打ちせず、`document.createElement`でJSがデータ駆動で描画する**（仕様書の要件通り）
- 各質問の`a`は「段落の配列」。配列の要素ごとに`<p>`になり、要素内の`\n`は`<br>`に変換される（原文の空行＝段落区切り、単一改行＝行内改行という対応）
- リンクプレースホルダー`＜○○（リンク）＞`は正規表現`／＜(.+?)（リンク）＞／`で検出し`<a href="#">○○</a>`に変換（`linkify()`関数）。実際の遷移先URLは未確定のためすべて`#`のまま

---

## 3. デザイン仕様・実測値

`Q&A.png`／`Q&A_アコーディオン開.png`をピクセル単位で実測。

| 要素 | 値 | 備考 |
|---|---|---|
| ページ背景 | `#030305` | 既存の`--color-bg`と同一 |
| カード背景 | `#1B232A` | 既存の`.hassui-bg`等と同一トークン（design.md 2.1相当） |
| カテゴリー見出し文字色 | `#FFFFFF`（`--color-heading`） | |
| 質問（Q）文字色 | `#797D83` | **ご予約特典ページの`.benefit-intro__title`と全く同じ値**。ブランド内で再利用されているグレーと判断 |
| 回答（A）文字色 | `#FFFFFF`（`--color-heading`） | |
| Qバッジ | 中抜き円、ボーダー色・文字色とも`#494949` | トップページ「detailed information」のナンバーバッジと同じ色 |
| Aバッジ | 塗りつぶし円、背景`#494949`・文字色白 | 同上 |
| 区切り線 | `--color-divider`（`#3A3A3A`） | カテゴリー間・Q間 |
| 見出し「question and answer」 | 欧文、`letter-spacing:0.3em`程度の広いレタースペース | 実測では正確な倍率算出が難しく、視覚的に近い値を採用 |
| contact usボタン | 円形120px、固定表示（`position:fixed; right/bottom`）、紫系グラデーション | 実測では画像内はほぼ均一な`#362847`寄りの色だったが、仕様書の「紫のグラデーション」という指示に従い`linear-gradient(145deg, #4c3f66, #2c2138)`を採用（既存のclose CTA `rgba(76,55,100,0.7)`と同系統の紫） |

> ⚠️ **カンプの解像度について**：`Q&A.png`は1920×2600で、`top.png`（スマホ幅を想定した想定デバイス375〜430px相当を高倍率でキャプチャ）とは異なり、**PC幅（デスクトップ）を想定したキャプチャである可能性が高い**（カードの左右余白比率・行の高さ等がモバイル基準にしては大きすぎるため）。そのため、PC幅の実装はこのカンプの実測値に基づき、SP幅は本サイトの一般的なモバイル対応方針（design.md 5章）に沿ってアシスタント側で独自に調整した。SP側の余白・フォントサイズは実カンプでの確認が取れていないため、**実機でユーザー確認後に微調整が必要な可能性がある**。

---

## 4. 実装メモ

### 4.1 開閉の仕組み（重要な技術的注意）

- **親カテゴリー**：1つだけ開く。あるカテゴリーを開くと、他の開いていたカテゴリー（とその中の開いていたQ）を自動的に閉じる。
- **子（Q）**：同一カテゴリー内で1つだけ開く。別のQを開くと前のQは自動的に閉じる。
- 開閉の高さアニメーションは**CSS Gridの`grid-template-rows: 0fr ↔ 1fr`テクニック**を使用（JSでscrollHeightを測定する方式ではない）。
  ```css
  .qa-category__panel { display: grid; grid-template-rows: 0fr; transition: grid-template-rows .35s ease; }
  .qa-category.is-open .qa-category__panel { grid-template-rows: 1fr; }
  .qa-category__panel-inner { min-height: 0; overflow: hidden; }
  ```
  > ⚠️ **既知の落とし穴**：グリッドアイテムはデフォルトで`min-height: auto`となり、コンテンツの本来の高さより縮まらない。**`min-height: 0`を明示しないと`0fr`が効かず、常に展開されたままになる**（このページの実装で実際に発生し、`data-reveal`のフェードインも巻き添えで発火しなくなる不具合につながった。IntersectionObserverのしきい値0.15に対し、常時展開状態でカードの高さが異常に大きくなり、画面内に入る割合が15%を下回っていたため）。
- +/−アイコンは`::before`（横棒・常時表示）と`::after`（縦棒）の2本の疑似要素で構成し、開いた状態で縦棒だけ`scaleY(0)`にすることで「+ → −」を滑らかにアニメーションさせる。
  > ⚠️ **選択したセレクタの注意**：`.qa-category.is-open > .qa-category__button .qa-toggle-icon::after`のように、**「開いている要素自身の直下のボタン」に限定してスコープする**必要がある。当初`.is-open .qa-question__button .qa-toggle-icon::after`という子孫セレクタで書いたところ、カテゴリーが開いている間はカテゴリー内の**すべてのQのアイコン**が「−」表示になってしまう不具合が発生した（`.is-open`は親カテゴリーに付与されるクラスのため、子孫セレクタでは範囲を絞り込めない）。

### 4.2 Astroのスコープ付きCSSが効かない問題（重要）

このページのアコーディオン本体（カテゴリー・Q・A）は仕様書の要件通り**`document.createElement`でJSが動的に生成**している。Astroのスコープ付きCSSは、ビルド時にテンプレートへ直接書かれた要素にしか`data-astro-cid-*`属性を付与できないため、**JSで動的生成した要素にはこの属性が付かず、通常の`<style>`（スコープ付き）のルールは一切マッチしない**（design.md 4.0で記録した`SectionHeading`の`set:html`と同じ系統の問題）。

このページでは対症療法的に`:global()`を個別に付けるのではなく、**`<style is:global>`でページ全体のスタイルをまるごとグローバル化**することで対応した（クラス名は`qa-`プレフィックスで衝突を避けている）。新しくデータ駆動でHTMLを生成するページを作る場合は、最初からこの方針で実装するとよい。

### 4.3 その他

- `BaseLayout`でラップ。Header/Footerは他ページと共通のまま自動的に流用される。**Back to Topは非表示**（`<BaseLayout showBackToTop={false} ...>`。design.md 4章参照。下層ページ共通の方針）。
- 見出し「question and answer / Q&A」は、ご予約特典ページの「pre-order benefit / ご予約特典」と同様に、大小2つのテキストを横並びにする専用マークアップ（`SectionHeading`コンポーネントは使わない）。下線罫はPCで右端まで伸ばす（`.cutting`等と同じ考え方）。
- `.section`・`.qa-card`等の左右余白はトップ/ご予約特典ページの`.section`と同じ考え方（SP 20px、PC 1200px中央寄せ+40px）を踏襲。
- close CTA・Purchase系CTAとは別の**4つ目の`.btn-circle`専用配色**として「contact us」ボタンを新規追加（`linear-gradient(145deg, #4c3f66, #2c2138)`）。Back to Topとは異なり**常時表示**（スクロール量による表示/非表示の切り替えなし）。

---

## 5. 未確定事項・要相談

1. **SP幅のレイアウトはカンプ未確認**：`Q&A.png`がPC幅想定のキャプチャである可能性が高く、SP幅の細かい余白・フォントサイズはアシスタント側の判断で実装した（3章の⚠️参照）。実機のSPカンプがあれば実測し直したい。
2. **リンク遷移先**：「問い合わせフォーム」「支払い方法について」「サイズ表」など、すべて`href="#"`のまま未確定。
3. **見出し・カード等のフォントサイズ・letter-spacingの精密実測**：現状は視覚的な近似値。正確な値が必要な場合は再測定する。
4. **「contact us」ボタンの遷移先**：現状`href="#"`のまま。問い合わせフォームやメーラー起動（`mailto:`）等、意図された遷移先を要確認。
5. ~~**サイト内からのリンク導線**~~：解決済み。トップページのフッター「question and answer」の`href`を`${base}/qa/`に変更済み。

### 5.1 追加調整（反映済み）

- 見出しサブラベル「Q&A」の文字色：`#FFFFFF`に変更（以前は`var(--color-caption)`）
- `.qa-card`の角丸：`border-radius:0`に変更（以前は16px）
- Q/Aバッジ：フォントを`Roboto Condensed`に変更、形状を正円→**楕円**（SP `24×32px`、PC `28×36px`。`border-radius:50%`のまま幅と高さを変えるだけで楕円になる）
- Qアコーディオン（内側）の+/−アイコン色：`#797D83`に変更（`.qa-question__button .qa-toggle-icon`にcolorを指定。カテゴリー側の+/−は白のまま変更なし）
- `.qa-badge--q`（Qバッジ）をAバッジと同じ見た目に統一：中抜き（ボーダーのみ）+グレー文字→**塗りつぶし背景`#494949`+白文字**に変更（QもAも同じ楕円バッジになった）
- 見出し下罫線のPC右伸長が効いていなかった不具合を修正：`.qa-heading`が`display:flex`のため、`.qa-heading__rule`もflexアイテムとして扱われ、`flex-shrink:1`（既定値）により`calc(100% + Npx)`の拡張分がコンテナ幅に収まるよう縮小されてしまっていた。`.qa-heading__rule`に`flex-shrink:0`を追加して解決（design.mdに一般化した注意点として追記推奨）

---

## 6. 次セッションTODO（優先度順）

1. 上記5章の未確定事項の確認・反映。
2. トップページのフッター等に`/qa/`への導線を追加するか検討。
3. SP幅の実機確認（1章参照）。
