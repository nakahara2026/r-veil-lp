# R VEIL — 「お支払い方法」ページ 設計書 (payment.md)

> このファイルは**下層ページ「お支払い方法」（`src/pages/payment.astro`）固有**の構成・実装方針をまとめたもの。全ページ共通のルール（カラー・タイポグラフィ・レイアウトの基本方針・共通コンポーネント仕様等）は[design.md](design.md)を参照。

> **ステータス**：実装済み（`src/pages/payment.astro`）。dev serverで表示確認済み。

## 1. ページ概要

| 項目 | 内容 |
|---|---|
| ページ名 | お支払い方法（payment） |
| URL | `/payment/`（独立した別ページ） |
| デザインカンプ | なし。ユーザーからのテキスト実装指示書（本ページ下部に転記）のみを基に実装 |
| 目的・ゴール | 利用可能な支払い方法（クレジットカード／電子決済サービス）の案内 |
| 遷移元 | フッターリンク一覧「payment」から`/payment/`へ遷移するよう実装済み（`Footer.astro`の`links`配列） |
| 遷移先（close） | ページ下部の円形ボタン「close」はトップページ（`/`）へ戻る通常リンク（ご予約特典ページのcloseと同じ考え方） |

---

## 2. ページ構成

### Section P1 — ヘッダー
- 見出し「payment」（欧文・レタースペーシング広め）＋サブラベル「お支払い方法」＋下線罫（PC限定で右端まで伸長）
- Q&Aページの`.qa-heading`と全く同じ構造・パターンを流用（`.payment-heading`として実装）

### Section P2 — コンテンツボックス
- 背景`#1B232A`（ページ背景`#030305`より一段明るいトーン）。`.benefit-intro`と同じ考え方で`margin:0 20px`を入れ、親要素（`.section`）より狭く中央寄りに配置
- ボックス内の構成（上から）：
  1. 本文リード文（白文字、2行）
  2. 「・クレジットカード」（グレー文字ラベル）＋カード名（白文字、ラベルよりやや大きめ）
  3. 「・電子決済サービス」（グレー文字ラベル）＋サービス名（白文字、ラベルよりやや大きめ）
  4. 補足注記「※SSL暗号化による...」（**白文字**。当初グレーで実装したが、ユーザー指摘により`#FFFFFF`に修正済み）

### Section P3 — close CTA
- 円形ボタン「close」→ トップページへ戻る通常リンク
- `.btn-circle`（design.md 3.4）を流用し、背景色は**`#3B2F4A`（ダークパープル系）専用トークン**として新規追加。ご予約特典ページのcloseボタン（`rgba(76,55,100,0.7)`）・Q&Aページのcontact us（`linear-gradient(145deg, #4c3f66, #2c2138)`）とは別の配色
- 配置：セクション内で`justify-content:flex-end`により右寄せ（ページ全体に対するfixedではなく、そのセクション内での右下配置）

---

## 3. Back to Topボタンについて（重要）

**このページではBack to Topボタンを表示しない**（ユーザー指示：「下層ページには、back to topのCTAは設置しません」）。

実装は`BaseLayout`に`showBackToTop`という真偽値propを追加し、`false`を渡すと`Footer.astro`内で`<button id="back-to-top">`自体がレンダリングされない仕組み（design.md 4章参照）。**ご予約特典ページ・Q&Aページも同時にこの対応を行い、Back to Topを非表示にした**（top.md/benefit.md/qa.mdも合わせて更新済み）。トップページ（`index.astro`）は`showBackToTop`を指定していない＝デフォルト`true`のままなので、従来通りBack to Topが表示される。

---

## 4. 実装メモ

- `BaseLayout`でラップ。Header/Footerは他ページと共通のまま自動的に流用される
- `.section`・見出し・下線罫伸長パターンはQ&Aページ（`qa.astro`）の実装をそのままコピーして流用
- リード文・ラベル・注記のフォントウェイトは`font-weight:300`（Light）を基本とする（ユーザー指示「フォントは全体的に細め」）

---

## 5. 未確定事項・要相談

1. **デザインカンプなし**：このページはテキスト指示書のみを基に実装しており、`top.png`等のような画像ベースの実測は行っていない。細かい余白・フォントサイズの最終確認が必要な場合がある。
2. **クレジットカード・電子決済サービスの対応状況**：Visa/Mastercard/JCB/American Express、Apple Pay/Google Pay/Shop Pay/PayPalという記載内容が実際の決済代行サービスの対応状況と一致しているか要確認。

---

## 6. 次セッションTODO（優先度順）

1. 上記5章の未確定事項の確認。
2. デザインカンプがあれば実測して細部を合わせる。

---

## 参考：ユーザーからの実装指示書（原文）

```
## 概要
モーダル/セクション形式で「お支払い方法」の内容を表示する。

## 1. ヘッダー部分
- 見出し英語表記：payment（小文字、文字間を広めにしたレタースペーシング）
- 見出し右側に日本語サブタイトル：「お支払い方法」（小さめフォント）
- 見出し下に、横幅いっぱいの薄い罫線（区切り線）を1本入れる

## 2. コンテンツボックス
- 背景色：#1b232a（ダークネイビー系）
- 外側の背景（モーダル全体）よりも一段明るいトーンにして、コンテンツボックスとして視認できるようにする
- 内側に十分なpaddingを確保

### ボックス内テキスト構成
1. 本文（通常テキスト、白文字）
2. 空行
3. 小見出し（グレー）＋本文（白文字）のペアを2組（クレジットカード／電子決済サービス）
4. 空行
5. 補足注記（グレー文字、小さいフォント）

## 3. 閉じるボタン
- 円形、背景色ダークパープル系（例：#3b2f4a相当）、テキスト「close」白文字小さめ、セクション右下配置

## 4. レイアウト全体の注意点
- 全体の背景は黒〜濃いネイビー系
- コンテンツボックスは横幅を親要素より狭くして中央寄りに配置、左右にマージン
- 縦のpaddingは詰まりすぎないよう十分に
- フォントは全体的に細め（light〜regular）
```
