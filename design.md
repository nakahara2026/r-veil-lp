# R VEIL — 全ページ共通 設計書 (design.md)

> このファイルは**サイト全体（トップページ・下層ページ問わず）に共通するルール**をまとめたもの。特定ページの構成・文言・実装詳細は各ページごとの `○○.md`（トップページは [top.md](top.md)、ご予約特典ページは [benefit.md](benefit.md)、Q&Aページは [qa.md](qa.md)、お支払い方法ページは [payment.md](payment.md)）を参照する。新規ページを実装する際は、このファイルを読んだうえで、対象ページの `○○.md` を作成/参照すること。

## 1. 概要

| 項目 | 内容 |
|---|---|
| ブランド名 | R VEIL |
| トーン | ダーク・モノトーン・タクティカル・ミニマル・高級感 |
| 想定デバイス | スマートフォン優先（1カラム、幅 375〜430px 相当で設計。PC版は中央寄せ＋余白拡張で流用）。この方針は全ページ共通 |
| 実装 | Astro（TypeScriptなし、`.astro`ファイルのみ）。静的サイト生成（`output: "static"`）。各ページ本体は`src/pages/`配下、`src/layouts/BaseLayout.astro`＋`src/components/Header.astro`・`Footer.astro`で全体を包む |
| 公開 | GitHub Pages（`https://nakahara2026.github.io/r-veil-lp/`）。`main`へのpushでGitHub Actionsが自動ビルド・公開 |

---

## 2. ブランド背景・コンセプト（Brand Story）

> デザイナーから共有された、ブランドの背景・思想に関する一次情報。特定のページのコピーではなく、**このブランドの全ページ（LP・下層ページ問わず）に共通する世界観・語彙・トンマナの根拠**となるもの。新規ページを実装する際は、本文コピーを書く/検証する前に必ずこの章を読み、ここで定義された語彙（Rain/Refined/Rare、VEIL、4つの美学の名称など）と矛盾しない表現になっているかを確認すること。

### 2.1 ブランド名の由来（Brand Name Origin）

**R VEIL（アール ヴェール）**

- **R** ： Rain（雨）、Refined（洗練された）、そして愛犬と過ごす Rare（希少な・唯一無二の）。
- ロゴ「∴」： 3滴の意味の表現。
- **VEIL** ： 優しく包み込む「ベール」。雨や汚れから愛犬を「守る」という意味と、纏う（まとう）ことで気品をプラスする「ドレスのベール」のような美しさを表現している。

> **由来に込めた想い**
> 「雨の日（Rain）も、いつものお散歩も、洗練された一枚のベール（VEIL）で包み込むように。」
> どんな天候の変化からも愛犬を優しく守り、日常の佇まいを美しく仕立てる。
> 雨（Rain）の日の憂鬱さえも、洗練（Refined）された美しさへと昇華させる。
> 愛犬のシルエットを優美に包み込む一枚の「ベール」でありたい。
> 大量生産では決して真似できない、一着一着に宿る希少性（Rare）と、大人の品格を表現した名である。

### 2.2 ブランドコンセプト（Brand Concept）

> 「静かに、美しく、雨を纏（まと）う。」
> 愛犬との時間は、何にも代えがたい特別なもの。
> だからこそ、纏う衣服にも、妥協のない美学とクオリティを。
>
> 『R VEIL』は、都市の風景に静かに溶け込む、シックで洗練されたドッグウェアとオーナーズバッグを提案するクチュールブランドです。
>
> 過剰な装飾を削ぎ落としたミニマルなデザイン、雨を跳ね返す高い機能性、そして犬の骨格を美しく見せるカッティング。私たちは、愛犬を子どもっぽく着飾るのではなく、一匹の「自立した美しいパートナー」として引き立てます。
>
> 私たちが目指したのは、いかにも「レインコート」らしい無骨なデザインではなく、街に溶け込む上質なデイリーウェア。日本の職人技が生み出す、圧倒的な機能性と美しさをお届けします。
>
> 流行を追わず、一着一着に職人の息吹を吹き込むため、すべてのコレクションは数量限定のシリアル生産（大量生産はいたしません）。目の届く範囲で、丁寧に仕立てられた日本製。本物を知るオーナー様と愛犬のための、特別な一着がここにあります。

### 2.3 『R VEIL』が守り抜く、4つの美学

1. **HIGH-FUNCTION MINIMALISM**
   普段着として美しい佇まいを保ちながら、突然の雨にも動じない高機能な撥水素材を採用。落ち着いたトーンの生地は、どんなシーンでも大人の気品を崩しません。
2. **ARCHITECTURAL CUTTING**
   愛犬の身体のラインを最も美しく見せるため、立体的なカッティング（裁断）を追求。ミリ単位で計算されたパターンは、窮屈さを感じさせない極上のフィット感を生み出します。
3. **MONOCHROME LINK**
   犬服と完全に同質・同素材で仕立てられた、オーナー様のためのプレミアムバッグ。愛犬と響き合う、シックで一体感のあるラグジュアリーな「リンクスタイル」を叶えます。
4. **LIMITED MADE IN JAPAN**
   私たちのこだわりは、大量生産をしないこと。日本の熟練した職人が、一針一針にクラフトマンシップを注ぎ込み、限定数のみを丁寧に仕立てます。それは、大量消費の時代に向けた、究極のサステナビリティと贅沢の形です。

> 各ページの本文コピーがこの章の語彙・言い回しと食い違う場合は、この章を正としてページ側を揃える（ページ固有の実装状況・差異メモは各ページの`○○.md`に記載する）。

---

## 3. デザイントークン

### 3.1 カラーパレット（ベーストークン）

| 用途 | カラー名 | HEX（近似） | 備考 |
|---|---|---|---|
| ベース背景 | Almost Black | `#030305` | セクション間の余白・本文背景・フッター最下部の帯 |
| ニュースバー/パネル背景 | Slate Navy | `#1B2326` | ヘッダー直下の告知バー、フッター本体 |
| 写真オーバーレイ（トーン統一用） | Slate Navy 60%透過 | `rgba(27, 35, 42, 0.6)` | 写真の上に重ねて落ち着いた雰囲気を出す共通オーバーレイ（`::after`擬似要素、画像自体は無加工） |
| アクセント（Back To Top） | Muted Mauve Gray | `#736C7E` | 「Back To Top」ボタン専用 |
| Purchase系CTAボタン | Muted Purple Gray 80%透過 | `rgba(76, 66, 90, 0.8)`（`#4C425A`の80%） | 円形の商品購入・予約CTAボタン専用（Back To Topとは別トークン。下層の商品ページでも同じ見た目のCTAとして再利用する） |
| 本文テキスト | Off White | `#E8E8E8` | 本文コピー |
| 見出し/ロゴ | Pure White | `#FFFFFF` | ロゴ、セクション見出し |
| キャプション/補足 | Warm Gray | `#8A8A8A` | 商品スペック、注記、フッターリンク |
| 区切り線 | Hairline Gray | `#3A3A3A` | 見出し横の細い罫線、点線区切り |

> 実写真が背景の大半を占めるページが多いため、パレットは「黒背景＋写真＋最小限のアクセント」という構成を基本とする。写真には共通フィルター `grayscale(5%) contrast(1.05) brightness(0.95)` を適用し、トーンを統一する。
>
> **ページ固有の背景色**（特定セクションだけで使う一回限りの色）は各ページの`○○.md`側に記載する。トップページの例は[top.md](top.md)、ご予約特典ページの例は[benefit.md](benefit.md)を参照。

### 3.2 タイポグラフィ（基本ルール）

| 要素 | ウェイト | サイズの考え方 | 字間 |
|---|---|---|---|
| セクション見出し（`<h2>`。`SectionHeading`コンポーネント、または直書きの`<h2>`） | Regular | **SP幅で大きくなりすぎないよう`clamp()`で可変にする**（`BaseLayout.astro`のグローバルCSSに`h2 { font-size: clamp(...); }`を基準として用意し、個別クラス指定がある場合はそちらが優先される） | 標準、下線罫を伴う（例外はページ側`○○.md`参照） |
| 背景写真オーバーレイの本文コピー | Regular | 16px / 22px 目安（SP/PC） | 行間 1.7〜1.8 |
| 通常の本文コピー | Regular | 16px（共通） | 行間 1.8 |
| 価格 | Medium | 20px（共通） | 標準 |
| ボタン内テキスト（Back To Top） | Regular | 11px / 13px | 標準・左寄せ／中央 |
| ボタン内テキスト（Purchase系CTA） | Regular | 11px | 左寄せ |

> **重要な決定事項1**：写真に重ねるオーバーレイ本文は、モバイル16px/PC22pxを既定値とする。
>
> **重要な決定事項2（h2タグ化）**：見出しは実際のセマンティックな`<h2>`タグで出力する（`<p>`に見た目だけh2相当のCSSを当てる実装にしない）。ページ全体のh1は**ヘッダーロゴのみ**（`<h1><a><img></a></h1>`）で、他の見出しは全てh2（h1の重複なし）。
>
> **重要な決定事項3（見出しのSPサイズはclamp化が基本）**：見出しのフォントサイズをSP/PCで固定値だけ持たせる（メディアクエリなしで常時同じpxを使う等）と、SP幅で見出しが大きすぎる見た目になりやすい。**「`clamp(最小値, 可変係数, 最大値)`で可変にする」という考え方を基本方針とする**。具体的な最小値・係数・最大値の数値はページ・要素ごとに異なるため、各ページの`○○.md`に記載する（トップページの例は[top.md](top.md)、ご予約特典ページの例は[benefit.md](benefit.md)を参照）。

#### SP限定の改行（`.sp-break`パターン）

本文・見出しの特定の位置だけSP幅で改行させたい（PCでは改行しない）場合の共通テクニック：

```html
テキストA<br class="sp-break" />テキストB
```

```css
/* SPが既定（改行される）。PCだけ非表示にして改行を打ち消す */
@media (min-width: 768px) {
  .sp-break { display: none; }
  /* SectionHeadingのtitleにset:htmlで埋め込む場合は :global(.sp-break) にする（4.0参照） */
}
```

`title`プロパティ等、`set:html`で描画されるHTML文字列の中に`<br class="sp-break">`を埋め込む場合は、Astroのスコープ付きCSSが効かないため必ず`:global()`で囲むこと（詳細は4.0章）。テンプレートに直接書く`<br>`（`set:html`を使わない箇所）はスコープ付きのままで問題ない。

### 3.3 レイアウト・グリッド（基本ルール）

- 1カラム構成、コンテンツ幅は画面幅に対して左右パディング 20px（モバイル）/ 40px（PC）。
- セクション区切りは「余白（黒背景の空白帯）」で表現。
- 画像は基本フルブリード（画面幅いっぱい）、テキストは画像の上に重ねるか、画像下に別パネルで配置。
- 見出しには下に細い横罫線（1px, `#3A3A3A`）。

#### 見出し罫線を画面端まで伸ばすテクニック

見出しの罫線だけを、コンテンツ幅（1200px中央寄せ）を超えて**画面の真の端まで片側だけ伸ばす**演出を使う場合がある（PC限定）。`.section-heading__rule`は既定で`width:100%`（親要素いっぱい）なので、`margin`のマイナス指定だけでは広がらない。**`width`自体を拡張する**方式を使う：

```css
/* 右に伸ばす場合 */
.target :global(.section-heading__rule) {
  width: calc(100% + max(40px, (100vw - 1200px) / 2 + 40px));
}

/* 左に伸ばす場合（右伸長と同じwidth拡張に加え、margin-leftを同量マイナスにする） */
.target :global(.section-heading__rule) {
  width: calc(100% + max(40px, (100vw - 1200px) / 2 + 40px));
  margin-left: calc(-1 * max(40px, (100vw - 1200px) / 2 + 40px));
}
```

> どのセクションの罫線をどちら方向に伸ばすかはページ・デザインカンプ次第。具体的な対象セクションの一覧は各ページの`○○.md`に記載する（トップページの例は[top.md](top.md)、ご予約特典ページの例は[benefit.md](benefit.md)を参照）。
>
> **⚠️ 親要素が`display:flex`の場合の注意**：見出しと罫線を横並びにするため親コンテナ（見出しラッパー）を`display:flex`にしているケースがある。この場合、罫線もflexアイテムとして扱われ、既定の`flex-shrink:1`により、`calc(100% + Npx)`で広げたはずの幅がコンテナの残り幅に収まるよう**縮小されてしまい、拡張が無効化される**（Q&Aページで実際に発生）。対象要素に`flex-shrink: 0;`を追加すること。



#### 全幅背景の実装パターン（重要・再利用中）

`.section`（max-width:1200px中央寄せ）を使うセクションで、**背景色だけを画面幅いっぱいに広げたい**場合は、以下のテクニックを使う：

```css
.target {
  background: #色;
  box-shadow: 0 0 0 100vmax #色;   /* 左右に無限に色を広げる */
  clip-path: inset(0 -100vmax);     /* 上下方向の広がりだけ打ち消し、隣接セクションへの色漏れを防ぐ */
}
```

> **⚠️ 過去に発生した不具合**：`clip-path`を付け忘れると、`box-shadow`は上下方向にも同じだけ広がってしまい、隣接する別セクションの背景に意図しない色（黒いライン、2色に見える等）が漏れて表示される。**この全幅背景テクニックを新規で使う際は、必ず`clip-path: inset(0 -100vmax)`をセットで書くこと。**

#### 写真スタック（2枚重ね）演出 ※現在未使用・要注意

同じ写真を、紙を重ねたように背面はぼかし・明るめのトーン、手前はシャープで右下方向にずらして重ねる演出。ご予約特典ページ（benefit.md）の告知パネル用に一度実装したが、**`benefit.png`実測の結果、そもそも1枚の写真自体が「左に無地の面／右に被写体」という構図を最初から持っていたことが判明し、2枚重ねの解釈自体が誤りと判明・撤回した**（3.3「パネルに写真を重ねる演出」参照。現在このパターンの実使用箇所はない）。将来的に本当に2枚の異なる写真を重ねたい場面が出てきた場合のみ、以下を参考にする：

```css
.stack__img {
  position: absolute;
  width: 82%;
  height: 82%;
  object-fit: cover;
  border-radius: 16px;
}
.stack__img--back {
  top: 0;
  left: 0;
  filter: blur(14px) brightness(1.3) saturate(0.9);
}
.stack__img--front {
  right: 0;
  bottom: 0;
}
```

> ⚠️ **教訓**：写真を重ねる演出に見えるデザインでも、実際には1枚の写真がすでにその構図（無地の面＋被写体）を持っているだけ、というケースがある。新規に「2枚重ね」を実装する前に、必ず元画像ファイル自体を確認すること。

#### パネルに写真を重ねる演出（negative margin）

色付きパネル（`.section`のカラー背景）の下部に、写真を重ねて配置する演出。ご予約特典ページの告知パネルで実測に基づき採用。**`position:absolute`ではなく、通常のドキュメントフロー上でnegative margin-topを使う**：写真がPCで画面の真の端まで伸びる必要がある（3.3「見出し罫線を画面端まで伸ばすテクニック」と同じ理由）場合、パネル自体が`max-width:1200px`等で幅を制約されていると、`position:absolute`で写真をパネルの子要素にしてしまうとその制約の内側に収まってしまう。**写真をパネルの外の兄弟要素のままにし、DOM順が後にあることを利用して自然にパネルより手前に描画させ、`margin-top`をマイナスにして引き上げる**ことで、幅の制約を受けずに重なりを実現する：

```css
.panel {
  padding-bottom: <写真の重なり量>; /* 写真が重なる分の高さを確保 */
}
.photo-block {
  margin-top: calc(-1 * <写真の重なり量>);
}
```

重なり量（パネルに重なる部分の高さ）と、パネル下端からのはみ出し量は、デザインカンプの実測比率に合わせる（ご予約特典ページでは写真の高さの約90%がパネルに重なり、残り10%がはみ出る。具体的な計算式は[benefit.md](benefit.md)参照）。

### 3.4 モーション/インタラクション（共通コンポーネント仕様）

| 要素 | 挙動 |
|---|---|
| Purchase系CTAボタン | 円形、背景`rgba(76,66,90,0.8)`、テキストは左寄せ11px。商品詳細のテキスト列の右下に絶対配置するのが基本パターン（座標などページ固有の実装値は各ページの`○○.md`参照）。**下層の商品ページでも同じ見た目・挙動のCTAとして再利用する共通コンポーネント** |
| Back to Top | 画面右下に固定表示（`position:fixed`）。枠線なし（`border:none`）。**ページ最上部（スクロール量300px以下）では非表示**（`opacity:0; pointer-events:none;`）にし、`window.scrollY > 300`でJSが`.is-visible`クラスを付けフェードイン表示する（`Footer.astro`のscriptで制御）。**Purchase系CTAと同様、全ページ共通の挙動とする** |
| スクロール演出 | `data-reveal`属性を付けた要素はスクロールで画面に入ると、フェードイン＋わずかな上方向スライドで表示される（`IntersectionObserver`, threshold 0.15）。画像列とテキスト列が隣接して並ぶセクションでは、両方に`data-reveal`を付けたうえで`transform: none`にし、リフト演出を打ち消してフェードのみにする方法もある（片方だけ・リフト付きだと、アニメーション中に上端がズレて「ガタつき」に見えるため） |

---

## 4. コンポーネント一覧（全ページ共通）

| コンポーネント | 役割・仕様 |
|---|---|
| `Header.astro` | ロゴは**画像**（`logo.png`）。`<h1><a href={base+"/"}><img src="logo.png"></a></h1>`。ページ内で唯一のh1。ロゴ高さはSP/PCでclamp・メディアクエリ等により可変（具体値は各ページ参照）。**`logo.png`は黒一色のアセット（白ピクセルなし）のため、`filter: invert(1)`で白反転させて表示している**（ロゴ配色トークンはPure White `#FFFFFF`。反転を忘れるとダーク背景で見えなくなる／トップページのヒーロー動画のように背景に明るい部分がある場合はたまたま視認できてしまうため気づきにくい不具合だった） |
| `Footer.astro` | フッターリンク一覧＋Back To Topボタンを内包。Back To Topの表示ロジック（3.4参照）もここで制御。リンクは`links = [{ label, href }, ...]`の配列（`base`変数を使い実URLを個別に設定できる。未確定のものは`href: '#'`のまま）。**Back To Topは`showBackToTop`プロパティ（既定`true`）で表示/非表示を切り替えられる**：`BaseLayout`が`showBackToTop`propを受け取り`<Footer showBackToTop={showBackToTop} />`として伝播する。`false`の場合`<button id="back-to-top">`自体がレンダリングされない（Footer側スクリプトは`document.getElementById`の結果に対しoptional chainingを使っているため、ボタンが存在しなくてもエラーにならない）。**下層ページ（ご予約特典・Q&A・お支払い方法）は`showBackToTop={false}`を指定し、Back To Topを表示しない運用**（トップページはpropを指定せず既定の`true`のまま） |
| `SectionHeading.astro` | `<h2>`＋任意のsubtitle＋下線罫（`.section-heading__rule`）を出力する共通見出しコンポーネント。`.section-heading`のmargin-bottomは**30px** |
| `BaseLayout.astro` | `<head>`・グローバルCSS（カラートークン、`h2`のclampベース値、リセットCSS等）・`data-reveal`の`IntersectionObserver`・**`.btn-circle`（円形CTAボタンの共通基盤：形状・境界線・トランジションのみを定義し、背景色は使う側で個別に指定する。Purchase系CTA・Back to Top・下層ページ独自のCTA<例：benefit.mdの「close」ボタン>はいずれもこれを継承）**を提供 |
| Product Gallery（カルーセル＋ドット＋サムネイル、`initGallery(prefix)`） | コート/バッグ（top.md）・ご予約特典（benefit.md）の2ページで実装・再利用を確認した共通パターン。**ただしAstroのCSSはファイルスコープのため、他ページで使う場合はCSS・JSともコピーして持ち込む必要がある**（4.1参照）。メイン画像・サムネイルの角丸は`overflow:hidden`の親要素だけでなく`<img>`自身にも同じ`border-radius`を明示する（Safari/WebKit系でobject-fit:coverのimgがGPU合成レイヤーに昇格し親のクリップが効かないことがあるための保険。親・img双方が同サイズであればどちらか一方の指定だけでも見た目は変わらず相互に冗長だが、どちらも残して問題ない） |
| spec-list・swatch-bar・`.note`・`.section__copy` | 商品詳細（size/fabric/color等）の共通表示パターン。同上、ページごとにCSSをコピーして使う |

### 4.0 `SectionHeading`の`title`にHTML（`<br>`等）を埋め込む場合の注意（重要・既知の不具合）

`SectionHeading.astro`は元々`<h2 ...>{title}</h2>`という書き方で`title`を出力していた（Astroの式展開はHTMLエスケープされるため、`title`文字列に`<br>`を含めても改行にならず、そのままテキストとして表示されてしまう）。SP限定の改行など`title`にHTMLを埋め込みたいケースのために、現在は`<h2 ... set:html={title} />`に変更済み。

**ここで踏んだ既知の不具合**：`set:html`で注入した要素（例：`<br class="sp-break">`）は、Astroのスコープ付きCSSが自動付与する`data-astro-cid-*`属性が**付与されない**（Astroの静的解析はテンプレート内に直接書かれた要素にしかスコープ属性を付けられず、`set:html`で実行時に注入される生HTML文字列の中身までは解析できないため）。そのため、呼び出し元ページ（例：`index.astro`）側のスコープ付き`<style>`で`.sp-break { display: none; }`のようなルールを書いても、Astroの変換後は`.sp-break[data-astro-cid-呼び出し元のID]`になり、スコープ属性を持たない`set:html`由来の要素には一致せず、ルールが効かない。

**対処**：`title`にHTMLを埋め込んで、かつそのHTML要素をCSSで制御したい場合は、そのCSSセレクタを`:global()`で囲んでスコープを外す（例：`:global(.sp-break) { display: none; }`）。同じ`title`文字列をテンプレートに直接書く場合（`set:html`を使わない通常のプレーンテキストの`title`）はこの問題は起きない。

> **一般化した教訓**：これは`SectionHeading`固有の問題ではなく、**Astroのスコープ付きCSSはビルド時にテンプレートへ直接書かれた要素にしか`data-astro-cid-*`を付与できない**という仕組み上の制約。`set:html`だけでなく、**`<script>`内で`document.createElement`等によりクライアントサイドで動的生成した要素も同様にスコープ属性が付かず、スコープ付き`<style>`のルールが一切効かない**（Q&Aページのアコーディオンで実際に発生。qa.md 4.2参照）。データ駆動でHTMLをJS生成するページでは、個別に`:global()`を付けて回るより、**そのページの`<style>`ブロックごと`<style is:global>`にしてしまう方が簡潔**（クラス名をページ固有のプレフィックスにしておけば他ページとの衝突は起きない）。

### 4.1 コンポーネント共有の実態（重要）

`Header.astro`・`Footer.astro`・`SectionHeading.astro`・`BaseLayout.astro`は実際に**インポートして共有**されるが、それ以外（Product Gallery、spec-list、swatch-bar、`.note`、全幅背景パターン、見出し罫線伸長パターン等）は**index.astro内にスコープされたCSSとして実装されており、他ページから直接参照できない**。新しい下層ページでこれらのパターンを使う場合は、該当するCSS（および必要なJS）を新しいページの`<style>`/`<script>`ブロックへ**コピーして持ち込む**運用になっている（プロジェクト全体で共通スタイルシートに切り出す構成にはなっていない）。ご予約特典ページ（benefit.md）の実装で、この運用が実際に機能することを確認済み。

---

## 5. レスポンシブ方針

- 基準はスマートフォン幅（〜430px）。PC/タブレットは2カラムブロックを維持しつつ最大幅1200pxに制限し中央寄せするのが基本（例外はページ側で個別に定義）。
- **フルブリード写真に`aspect-ratio`を指定する場合は、必ず元画像の実寸比率を確認してから設定する**。実寸と異なる比率を指定すると`object-fit: cover`で意図せず大きくクロップされる。
- **全幅背景色が欲しい場合は3.3の`box-shadow`+`clip-path`パターンを使う**（`clip-path`を忘れると隣接セクションに色が漏れる）。

### 5.1 PC版仕様

- コンテンツ最大幅：1200px（左右パディング40px、中央寄せ）。フルブリード要素は制約を受けない。

---

## 6. 公開・デプロイ

> **本番移行の予定**：サイト完成に伴い、GitHub Pagesからレンタルサーバー（FTP）への移行を予定している。移行に必要な作業手順・注意点はAGENTS.md 6章にまとめてある（接続情報等の機密情報は含まれないよう配慮している）。

- `astro.config.mjs`：`site: 'https://nakahara2026.github.io'` / `base: '/r-veil-lp'`（レンタルサーバー移行後は本番ドメインのルート公開用に変更する。AGENTS.md 6章参照）
- 画像・favicon・内部リンクは全て`import.meta.env.BASE_URL`基準の相対パス（`const base = import.meta.env.BASE_URL.replace(/\/$/, '')`）
- GitHub Actions（`.github/workflows/deploy.yml`）：`main`へのpushで`withastro/action`→`actions/deploy-pages`。リポジトリ側で**Settings → Pages → Source を "GitHub Actions"**にする必要あり
- `top.png`（デザインカンプ）は`.gitignore`で除外

### 6.1 開発時の注意（dev serverのHMR staleness）

**devサーバーを長時間起動したまま多数のコンポーネントファイルを編集すると、HMR（ホットリロード）が古いスタイルをキャッシュしたままになり、ソースコードは正しいのに実機では反映されないことがある。**

**対処法**：ブラウザで確認して「コードは合っているはずなのに反映されない」と感じたら、まず以下でdevサーバーを再起動してから再確認する。

```bash
npx astro dev stop
npx astro dev --background
```

`npm run build`（本番ビルド）は毎回フルビルドのため、この問題は発生しない。挙動が疑わしい時はビルド後のdistを確認するのも有効。

### 6.2 デザインカンプ（`top.png`等）の実測ワークフロー

新規セクション実装・レイアウトの疑義がある場合は、必ずデザインカンプ画像をPythonの`PIL`でクロップ＋グリッド線を重ねて実測してから着手する（目測で実装すると手戻りが発生しやすい）。カンプ画像自体は`.gitignore`で除外されているため、リポジトリには含まれない。

---

## 7. 備考

- 犬種の一貫性・モデル起用の権利関係は別途素材提供元に確認が必要（全ページ共通の留意事項）。
- 各ページの構成・文言・アセット状況・TODOは、それぞれのページの`○○.md`（トップページは[top.md](top.md)）を参照。新規ページを作る際は、このファイルの内容を踏まえたうえで同様の専用ファイルを用意する。
